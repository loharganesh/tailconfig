.sidenav {
	@apply w-max flex flex-col gap-xs p-sm
    sm:w-[248px];
}

.sidenav-item {
	@apply px-sm py-xs flex items-center gap-sm rounded-primary text-title cursor-pointer
    /* Hover */
    hover:bg-secondary/50;
}

.sidenav-item.active {
	@apply bg-secondary;
}

.sidenav-item-text {
	@apply hidden
    sm:block;
}
